apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service2.service.name }}
spec:
  type: {{ .Values.service2.service.type }}
  selector:
    app: {{ .Values.service2.service.app }}
  ports:
    - protocol: {{ .Values.service2.service.protocol }}
      port: {{ .Values.service2.service.port }}
      targetPort: {{ .Values.service2.service.targetPort }}